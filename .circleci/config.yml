version: 2.1
orbs:
  node: circleci/node@1.1
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      -node/install-packages:
        - node/with-cache
      - run: npm install
      - run: npm run test
workflows:
  version: 2.1
  test:
    jobs:
      - test
